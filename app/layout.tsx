import './globals.css'; import Navbar from '../components/Navbar'; export const metadata={title:'SimpliTrade — Learn to Trade First'}; export default function RootLayout({children}:{children:React.ReactNode}){return(<html lang='en'><body><Navbar/><main className='container'>{children}</main><footer style={{padding:24,opacity:.7}}>© {new Date().getFullYear()} SimpliTrade</footer></body></html>) }